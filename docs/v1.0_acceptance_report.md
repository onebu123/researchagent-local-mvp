# ResearchAgent v1.0 验收报告

## 当前结论

ResearchAgent v1.0 Local MVP 已通过本地发布验收。该版本适合本机演示、功能评估和导出审计材料，不代表 production-ready、peer-review-ready 或合规认证状态。

## 覆盖范围

- 保留 v0.1 到 v0.10 的历史 validation 脚本。
- 新增 `scripts/validate_v1.py` 发布门禁入口。
- 新增项目 zip 导出工具、API 和前端面板。
- 新增 Local MVP Overview、Release Readiness、Project Export 前端入口。
- 新增 demo reset、本地启动说明脚本和 v1.0 用户文档。

## 已验证命令

- `python -m compileall services\api scripts`：通过。
- `python -m pytest services\api\tests`：通过，97 passed。
- `python scripts\reset_demo.py --yes`：通过。
- `python scripts\export_project_zip.py --project-id demo_project`：通过。
- `cd apps\web && npm run typecheck`：通过。
- `cd apps\web && npm run build`：通过。
- `cd apps\web && npm audit`：通过，0 vulnerabilities。
- `cd apps\web && npx playwright test`：通过，8 passed。
- `python scripts\validate_v1.py`：通过，输出 `ResearchAgent v1.0 Local MVP validation passed.`。

## 导出结果

- 最新 demo 导出：`projects/demo_project/exports/researchagent_demo_project_local_mvp_export_20260606T064448Z.zip`
- zip 已验证包含 manuscript、provenance、reviews、trust、analysis、figures、literature metadata、audit export 和 run history。
- zip 已验证不包含 `.env*`、密钥、缓存、运行时目录、绝对路径、`.next`、`node_modules` 或 Playwright 报告。

## 说明

`scripts/validate_v1.py` 使用当前发布门禁命令和 v1 专项断言做汇总验收。旧版 `validate_v01.py` 到 `validate_v10.py` 继续保留，但这些脚本存在递归调用前序版本的历史结构，v1 汇总入口不再重复递归执行它们。
